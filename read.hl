#include lib.std
#include lib.math

def main(argc:int, argv:ptr) -> int {
	int n = 0
	while(n < 42) {
		$prints("fibonacci at ")
		$print(n)
		$prints(" is - ")
		$println($fib(n))
		n = n + 1
	}
	return 0
}
